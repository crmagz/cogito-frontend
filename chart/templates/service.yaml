apiVersion: v1
kind: Service
metadata:
  name: {{ include "cogito-workbench.fullname" . }}
  labels:
    {{- include "cogito-workbench.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "cogito-workbench.selectorLabels" . | nindent 4 }}
