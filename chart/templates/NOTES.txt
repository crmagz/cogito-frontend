The Cogito Workbench is available through the {{ include "workbench.fullname" . }} service.

For local cluster access:
  kubectl -n {{ .Release.Namespace }} port-forward service/{{ include "workbench.fullname" . }} 8001:{{ .Values.service.port }}

Session relay mode requires an OIDC-aware relay at relay.session.upstreamUrl.
